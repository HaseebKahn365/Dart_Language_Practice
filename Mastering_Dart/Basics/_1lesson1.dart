//Hot Reload: updated code and ui updates in real time.
//AOT: used in production, compiles dart code to native machine code.
//JIT: used in development, compiles dart code to intermediate code.
//Dart is a statically or strongly typed language which means that the types of the variables are checked before compile time. This feature is called sound type system.

void main() {
  // print('Hello world!');
  // String name = 'Haseeb';
  // int age = 19;
  // name = name.toUpperCase();

  // print(name + ' is $age years old.');
  // print('twice my age is ${age} + ${age} = ${age + age}');

  // String lovePizza = 'I love pizza';
  // print(lovePizza.contains('love'));
  // String loveChipts = lovePizza.replaceAll('pizza', 'chips');
  // print(loveChipts);

  // String ageString = 'hi' + age.toString();
  // print(ageString);
  // double pi = 3.14;
  // print(pi.toStringAsFixed(0));
  // int a = 3.14.toInt();
  // a = 3.14.toInt();
  // double b = a.toDouble();
  // print(double.parse(ageString));

  // int celcius = 36;
  // double forenheit;
  // forenheit = 1.8 * celcius + 32;
  // print(forenheit.toStringAsFixed(0));

  //Boolean Algebra:
  print(0x10);
  int green = 0xff00ff00;
  print(green.toRadixString(8));
  int a = 9;
  print(a << 1);
}
